# Local API keys (copy settings.example.json → settings.json)
.claude/settings.json
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
env/

# Local YAML overrides (defaults live in config.default.yaml)
config.yaml

# Local session instructions (copy from instructions.example.json)
instructions.json

# Env files
.env
.env.*
!.env.example

# Paper library (local; per-project under papers/<slug>/ — regenerated by downloads)
papers/

# Pipeline resume checkpoints (per prompt fingerprint)
pipeline_state/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
